# Overhead run, closest to the final topology: the same 4 "3.5 flash hard" environments x 8
# platform rollouts every step, 10 steps, sequences up to 256k tokens. 8 H200 at TP 4 x CP 2
# (GDN context parallelism needs the default 'fla' backend); with CP, max tokens per GPU is
# about the longest sequence / cp_size. Log-probs and the loss are chunked/recomputed so the
# 256k x vocabulary logits never materialize at once. Otherwise matches ../train_args.txt.
--actor-num-nodes 1
--actor-num-gpus-per-node 8
--calculate-per-token-loss
--rollout-batch-size 4
--global-batch-size 32
--num-rollout 10
--tensor-model-parallel-size 4
--sequence-parallel
--pipeline-model-parallel-size 1
--context-parallel-size 2
--expert-model-parallel-size 1
--expert-tensor-parallel-size 1
--use-dynamic-batch-size
--max-tokens-per-gpu 131072
--recompute-granularity full
--recompute-method uniform
--recompute-num-layers 1
--log-probs-chunk-size 4096
--recompute-loss-function
--advantage-estimator grpo
--kl-loss-coef 0.00
--kl-loss-type low_var_kl
--kl-coef 0.00
--entropy-coef 0.00
--eps-clip 0.2
--eps-clip-high 0.28
--optimizer adam
--lr 1e-5
--lr-decay-style constant
--weight-decay 0.1
--adam-beta1 0.9
--adam-beta2 0.98
--attention-dropout 0.0
--hidden-dropout 0.0
--accumulate-allreduce-grads-in-fp32
--attention-softmax-in-fp32
--attention-backend flash
--save /state/checkpoints
--save-interval 1
--seed 42
--use-wandb
--wandb-project miles-qwen38
--wandb-group qwen38-27b-overhead
